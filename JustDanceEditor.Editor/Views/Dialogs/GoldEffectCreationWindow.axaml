<Window xmlns="https://github.com/avaloniaui"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        x:Class="JustDanceEditor.Editor.Views.Dialogs.GoldEffectCreationWindow"
        xmlns:vm="clr-namespace:JustDanceEditor.Editor.ViewModels.Dialogs;assembly=JustDanceEditor.Editor"
        x:DataType="vm:GoldEffectCreationViewModel"
        Title="Add Gold Effect"
        Width="380"
        SizeToContent="Height"
        CanResize="False"
        WindowStartupLocation="CenterOwner">
  <Grid Margin="6">
    <Grid.ColumnDefinitions>
      <ColumnDefinition Width="120"/>
      <ColumnDefinition Width="*"/>
    </Grid.ColumnDefinitions>
    <Grid.RowDefinitions>
      <RowDefinition Height="Auto"/>
      <RowDefinition Height="Auto"/>
      <RowDefinition Height="Auto"/>
    </Grid.RowDefinitions>

    <TextBlock Grid.Row="0" Grid.Column="0" VerticalAlignment="Center" Text="Duration (beats):"/>
    <NumericUpDown Grid.Row="0" Grid.Column="1" Width="120" Value="{Binding DurationBeats, Mode=TwoWay}"/>

    <TextBlock Grid.Row="1" Grid.Column="0" VerticalAlignment="Center" Text="Effect Type (int):"/>
    <NumericUpDown Grid.Row="1" Grid.Column="1" Width="120" Value="{Binding EffectType, Mode=TwoWay}"/>

    <StackPanel Grid.Row="2" Grid.Column="0" Grid.ColumnSpan="2" Orientation="Horizontal" HorizontalAlignment="Right">
      <Button Name="OkBtn" Margin="6">OK</Button>
      <Button Name="CancelBtn" Margin="6">Cancel</Button>
    </StackPanel>
  </Grid>
</Window>